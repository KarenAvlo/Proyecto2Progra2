#pragma once

#include <string>

void limpiarPantallaPortable();
int obtenerOpcion(int min, int max);
std::string obtenerEntrada(const std::string& mensajeP);
int obtenerNumero();
int obtenerMes();
int obtenerDia(int mes);